fastapi==0.115.0
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pinned below 4.1: passlib 1.7.4 (last release, unmaintained) does an
# internal self-test on import that assumes bcrypt still silently
# truncates secrets over 72 bytes. bcrypt>=4.1 removed that silent
# truncation and raises ValueError instead, which breaks passlib's
# backend detection entirely (see https://github.com/pyca/bcrypt/issues/684
# and https://foss.heptapod.net/python-libs/passlib/-/issues/183).
bcrypt==4.0.1
python-multipart==0.0.9
celery==5.4.0
redis==5.0.8
httpx==0.27.2
Jinja2==3.1.4
reportlab==4.2.2
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
email-validator==2.2.0
pyotp==2.9.0
