fastapi==0.115.0
uvicorn[standard]==0.52.4
SQLAlchemy==2.0.52
alembic==1.13.2
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings==2.15.0
python-jose[cryptography]==3.3.0
# No passlib: see the module docstring in app/core/security.py for why -
# passlib 1.7.4 (unmaintained since 2020) is broken against any bcrypt
# release >=4.1, so password/OTP hashing calls bcrypt directly instead.
# bcrypt itself ships an abi3 (stable ABI) wheel, so a current version
# works across Python releases - including brand new ones - without
# needing a version-specific pin.
bcrypt==5.0.0
python-multipart==0.0.9
celery==5.4.0
redis==5.0.8
httpx==0.27.2
Jinja2==3.1.4
reportlab==5.0.1
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
email-validator==2.2.0
pyotp==2.9.0
